// See README.md: this module exists only so the version of the parser
// generator CI runs has a home Dependabot can bump. Nothing imports it.
module github.com/PelicanPlatform/classad/citools

go 1.26.0

require golang.org/x/tools v0.50.0
